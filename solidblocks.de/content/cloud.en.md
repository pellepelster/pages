---
title: "Solidblocks Cloud"
date: 2024-02-20T22:00:00+01:00
draft: false
carousel:
  - src: "/features/quickstart.cast_scaled.gif"
    caption: "Solidblocks Cloud is a standalone CLI that deploys managed services on bare virtual machines, networking and storage. No container schedulers, no cloud specific services, just plain virtual machines running services on Linux, managed via SSH."
  - src: "/features/quickstart_s3.cast_scaled.gif"
    caption: "To protect your data, Solidblocks includes automatic backup and restore functionality. Besides backup to locally attached storage backups can also be written to S3 buckets, allowing for a complete disaster recovery of the system state from S3."
  - src: "/features/quickstart_dns.cast_scaled.gif"
    caption: "If the configured cloud provider supports DNS, switching to DNS based endpoints secured by SSL certificates instead of IP addresses is as easy as adding the root domain to the config file."
  - src: "/features/quickstart_secrets.cast_scaled.gif"
    caption: "Strong credentials are generated for all deployed services and automatically added to the configured secret provider. Rotating a password becomes as easy as updating the secret and triggering a new apply."
  - src: "/features/quickstart_help.cast_scaled.gif"
    caption: "Context-sensitive information and error messages help connecting to the deployed services."
---

{{< feature-carousel >}}
